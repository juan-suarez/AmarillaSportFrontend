
import { Auth } from "@/gateway/auth";
import styles from "./page.module.css";
import { Product } from "@/gateway/products";
import { cookies } from "next/headers";

export default async function Home() {
  return (
    <main className={styles.main}>
      products
    </main>
  );
}
